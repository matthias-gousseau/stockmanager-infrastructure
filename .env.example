# Configuration pour l'environnement de développement (valeurs par défaut)
# Ces valeurs sont utilisées si les variables d'environnement ne sont pas définies

# Database Development (valeurs par défaut dans le docker-compose)
DB_USER_DEV=postgres
DB_PASSWORD_DEV=postgres
JWT_SECRET_DEV=dev_secret_not_for_production

# Configuration pour l'environnement de production
# Ces valeurs DOIVENT être fournies via GitHub Secrets lors du déploiement

# Database Production (passées via GitHub Secrets)
# DB_USER_PROD=your_db_user
# DB_PASSWORD_PROD=your_secure_password
# JWT_SECRET_PROD=your_jwt_secret_key_here

# Note: En production, ces valeurs sont injectées automatiquement par le workflow CI/CD
# Vous n'avez PAS besoin de créer un fichier .env sur le serveur